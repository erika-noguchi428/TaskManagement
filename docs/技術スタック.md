# タスク管理アプリ 技術スタック

本書は [要件定義書](./要件定義書.md) の「8. 技術選定」を詳細化したものである。保守・引き継ぎの材料となるよう、実装に使用する技術要素を明記する。

バックエンドにJava/SpringBoot、フロントエンドにReact、データベースにPostgreSQLを使用する方針が確定したことに伴い、旧来のVanilla JS + localStorage構成から全面的に刷新した。

---

## 1. フロントエンド

| 項目 | 選定内容 | 備考 |
|---|---|---|
| プログラミング言語 | JavaScript (ES6+) | これまでの方針(型付き言語は使用しない)を踏襲し、TypeScriptは導入しない。将来的に型安全性が必要になった場合はTypeScript移行を検討する |
| フレームワーク | React | Next.jsは今回のスコープ外とし、React単体でSPA(Single Page Application)として実装する |
| ビルドツール | Vite | Create React Appは非推奨のため、現行の標準的な選択肢であるViteを採用する |
| パッケージマネージャー | npm | Node.js標準のパッケージマネージャーであり追加導入が不要なため |
| HTTPクライアント | Axios | バックエンドのREST APIとの通信に使用する |
| スタイリング | Tailwind CSS | 指定により採用。ユーティリティクラスをJSX内に直接記述する構成とし、別途CSSファイルの命名規則を考える必要がない |
| テスト | Vitest / React Testing Library | Viteとの親和性が高いVitestを採用する |

---

## 2. バックエンド

| 項目 | 選定内容 | 備考 |
|---|---|---|
| プログラミング言語 | Java 25 (LTS) | 長期サポート版であり、SpringBoot 4系の推奨バージョンであるため |
| フレームワーク | Spring Boot 4.x | REST APIの実装にSpring Web(Spring MVC)を使用する |
| ビルドツール | Gradle | 指定により採用。Groovy DSLまたはKotlin DSLでビルド定義を記述する |
| データアクセス | Spring Data JPA (Hibernate) | エンティティとテーブルのマッピング、リポジトリ層の実装に使用する |
| バリデーション | Spring Validation (Bean Validation / Hibernate Validator) | タイトル必須等の入力チェックをAPI層で行う |
| DBマイグレーション | Flyway | テーブル定義をバージョン管理されたSQLで管理し、アプリ起動時に自動適用する |
| テスト | JUnit 5 / Spring Boot Test / Mockito | ユニットテスト・統合テストに使用する |

---

## 3. データベース

| 項目 | 選定内容 | 備考 |
|---|---|---|
| DB製品 | PostgreSQL | 指定により採用 |
| ローカル環境構築 | Docker / Docker Compose | ローカル開発用にPostgreSQLコンテナを起動する構成とする。個別インストールの手間を省く |
| スキーマ管理 | Flywayによるマイグレーションファイル(SQL) | `src/main/resources/db/migration` 配下でバージョン管理する |

---

## 4. フロントエンド・バックエンド間の連携

| 項目 | 選定内容 | 備考 |
|---|---|---|
| 通信方式 | REST API (JSON) | React(開発時: `http://localhost:5173`)からSpring Boot(`http://localhost:8080`)のAPIを呼び出す構成 |
| CORS設定 | Spring Boot側で開発用オリジンを許可 | フロントエンド・バックエンドを別プロセスで起動するため設定が必要 |

---

## 5. 主要な機能の実装方針

| 機能 | 使用する技術 | 備考 |
|---|---|---|
| ドラッグ&ドロップ | dnd-kit | Reactの仮想DOM上でブラウザ標準のHTML5 Drag and Drop APIを直接扱うと状態管理が煩雑になるため、Reactと親和性が高く現行で活発にメンテナンスされているdnd-kitを採用する |
| データ永続化 | PostgreSQL(バックエンド経由) | ブラウザのlocalStorageではなく、REST API経由でDBに保存する構成に変更した |
| 日付入力 | HTML標準の `<input type="date">` | React内でも標準のまま使用し、専用の日付選択ライブラリは導入しない |

---

## 6. バージョン管理

| 項目 | 選定内容 | 備考 |
|---|---|---|
| バージョン管理 | Git / GitHub | ソースコードの管理に使用する |

---

## 7. 技術選定の理由

- バックエンドはJava/SpringBoot(ビルドツールはGradle)、フロントエンドはReact(Next.jsは今回対象外、スタイリングはTailwind CSS)、データベースはPostgreSQLという指定を前提とした
- 上記以外のツール(ORM・マイグレーション・DnDライブラリ等)は、指定されたフレームワーク・言語のエコシステムで標準的・広く使われているものを選定し、学習効果と情報の得やすさを優先した
- 個人開発・学習目的のプロジェクトであり大規模なトラフィックは想定しないため、認証基盤や分散システム向けの技術(メッセージキュー等)は導入しない
- 将来的な拡張(複数ユーザー対応等)に備え、フロントエンド・バックエンド・DBを分離した構成としたが、現時点ではスコープ外の機能(認証等)は実装しない
