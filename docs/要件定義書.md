# タスク管理アプリ 要件定義書

## 1. 概要

### 1.1 目的
自分専用のシンプルなタスク管理アプリを作成する。Trello風に、タスクを「未着手・作業中・完了」の3つの進捗状況に分けて管理し、ドラッグ&ドロップで進捗を更新できるようにする。

### 1.2 想定利用者
- 開発者本人のみ(1人で使用)
- ログイン機能・複数ユーザー対応は不要

### 1.3 開発背景
スクールの課題として着手。まずは要件定義を行い、その後設計・実装に進む。

---

## 2. 機能要件

### 2.1 カード(タスク)のCRUD操作

| 機能 | 内容 |
|---|---|
| 作成 | タイトル・説明文・優先度・期限を入力してカードを新規作成する |
| 編集 | 作成済みのカードの内容(タイトル・説明文・優先度・期限)を修正する |
| 削除 | 不要になったカードを削除する |

### 2.2 カードの移動・並び替え

| 機能 | 内容 |
|---|---|
| 列間移動 | ドラッグ&ドロップで「未着手」「作業中」「完了」の列(カラム)間をカードを移動できる |
| 並び替え | 同じ列内でドラッグ&ドロップによりカードの表示順を並び替えできる |

### 2.3 進捗状況(ステータス)の列構成
列(カラム)は以下の3つで固定とする。ユーザーによる列の追加・編集・削除は不要。

1. 未着手
2. 作業中
3. 完了

### 2.4 表示に関する機能
- 優先度が一目でわかるように、色分けなどで表示する
- 期限が近い、または過ぎているカードを強調表示する

### 2.5 データの永続化
- ブラウザを閉じても、再度開いたときに登録済みのカード情報が残っていること

---

## 3. カード(タスク)のデータ項目定義

| 項目名 | 必須/任意 | 型 | 備考 |
|---|---|---|---|
| タイトル | 必須 | 文字列 | 空では作成不可 |
| 説明文 | 任意 | 文字列(複数行) | 未入力可 |
| 優先度 | 必須 | 高 / 中 / 低 の選択式 | |
| 期限 | 任意 | 日付 | |
| ステータス | 必須 | 未着手 / 作業中 / 完了 | カード作成時のデフォルトは「未着手」 |
| 並び順 | システム管理 | 数値など | 同一列内の表示順を保持するための内部データ |

※担当者(アサイン)の項目は持たない(1人利用のため不要)。

---

## 4. 非機能要件

- サーバー・データベースを別途構築しない(ブラウザの localStorage にデータを保存する)
- インターネット接続不要でローカルで動作する
- 特別なインストール作業なしに、ブラウザで開けば使える

---

## 5. スコープ外(今回は対応しない事項)

- 複数ユーザーでの共有・ログイン機能
- 担当者(アサイン)機能
- 列(カラム)自体の追加・削除・名称変更
- スマートフォン等、別端末とのデータ同期(あくまで同一ブラウザ内での保存)
- 通知機能(期限が近づいたときのアラート等)

---

## 6. 技術構成(想定)

- HTML / CSS / JavaScript のみで実装
- データ保存: ブラウザの localStorage
- サーバー・DB: 不要

---

## 7. 今後の流れ

1. 要件定義(本ドキュメント) ← 完了
2. 画面設計・データ設計
3. 実装
4. 動作確認
